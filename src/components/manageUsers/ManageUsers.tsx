import {Text, View} from "react-native";
import {Props} from "src/types";

export const ManageUsers = ({navigation, route}: Props<"ManageUsers">) => {
  return (
    <View>
      <Text>Manage users soon</Text>
    </View>
  );
};

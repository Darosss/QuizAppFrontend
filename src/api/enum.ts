export enum BaseEndpoints {
  HOME = "/",
  QUIZES = "quizes/",
  QUESTIONS = "questions/",
  ANSWERS = "answers/",
  SUBMISSIONS = "submissions/",
  AUTH = "auth/",
}

export enum UserRolesType {
  USER = "user",
  ADMIN = "admin",
  SUPER_ADMIN = "super_admin",
}
